@model UserViewModel

@{
    ViewData["Title"] = "UserEdit";
    Layout = "~/Views/Member/_MemberLayout.cshtml";
}


@if (ViewBag.success == "true")
{   <div class="col-md-4 offset-md-4">
        <h2>Bilgi Güncelleme</h2>
        <div class="alert alert-info">
            <span>Bilgileriniz güncellenmiştir.</span>
        </div>
    </div>
}
<div class="row">
    <div class="col-md-4 offset-md-4">
        <h2>Bilgi Güncelleme</h2>
        <form asp-action="UserEdit" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                <input asp-for="UserName" class="form-control" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PhoneNumber" class="control-label"></label>
                <input asp-for="PhoneNumber" class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="BirthDay" class="control-label"></label>
                <input asp-for="BirthDay" class="form-control" />
                <span asp-validation-for="BirthDay" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="City" class="control-label"></label>
                <input asp-for="City" class="form-control" />
                <span asp-validation-for="City" class="text-danger"></span>
            </div>


            @*select, asp-items="@ViewBag.Gender"
            1.yöntem backend ile, ViewBag.Gender kullanıldı
            *@
            <div class="form-group">
                <label asp-for="Gender" class="control-label"></label>
                <select class="form-control" asp-for="Gender" asp-items="@ViewBag.Gender"></select>
            </div>
            
            @*              
            2.yöntem frontend ile, Html.GetEnumSelectList kullanıldı
            
            <div class="form-group">
                <label asp-for="Gender" class="control-label"></label>
                <select class="form-control" asp-for="Gender" asp-items="Html.GetEnumSelectList<Semerkand_Dergilik.Enums.Gender>()"></select>
            </div>

            asp-for : Model (UserViewModel) içerisindeki gender'e karşılık gelir. Kullanıcının gender bilgisidir
            asp-items : dropdownda listelenecek olan itemlar enumdan gelir
            *@

           <label class="control-label">Resim</label>
           <input class="form-control" type="file" name="userPicture" src="~/userpicture/user.webp" />    

@*            @if (String.IsNullOrEmpty(Model.Picture))
            {
                <label class="control-label">Resim</label>
                <input class="form-control" type="file" name="userPicture" src="~/userpicture/user.webp" />               
            }*@
@*            else
            {
                @*type="file", name="userPicture" 
                <div class="form-group">

                    <label class="control-label">Resim</label>
                    <input class="form-control" type="file" name="userPicture" src="~/userpicture/@Model.Picture" />
                </div>
            }*@


            <div class="form-group mt-2">
                <input type="submit" value="Güncelle" class="btn" style="background-color:#3498DB; color: white;" />
            </div>
        </form>
    </div>
</div>