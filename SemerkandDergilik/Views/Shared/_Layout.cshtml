<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <script src="~/lib/jquery/jquery.js"></script>
    <script src="~/lib/jquery-validate/jquery.validate.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js"></script>

    <link href="~/lib/twitter-bootstrap/css/bootstrap.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Semerkand_Dergilik.styles.css" asp-append-version="true" />
    <link href="~/lib/twitter-bootstrap/css/bootstrap.css" rel="stylesheet" />
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
        <div class="container">
            <a class="navbar-brand" asp-action="Index" asp-controller="Home">Semerkand Dergilik</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">

                @*                    <ul class="navbar-nav ">
                <li class="navbar-nav">
                <a class="nav-link" asp-action="SignUp">Üye Ol</a>
                </li>
                <li class="navbar-nav">

                <a class="nav-link" asp-action="LogIn">Giriş yap</a>
                </li>
                </ul>*@

                @if (!(User.Identity.IsAuthenticated))
                {
                    <ul class="navbar-nav ms-auto">

                        <li class="navbar-nav">
                            <a class="nav-link" asp-action="SignUp">Üye Ol</a>
                        </li>
                        <li class="navbar-nav">

                            <a class="nav-link" asp-action="LogIn">Giriş yap</a>
                        </li>
                    </ul>
                }
                @if (User.Identity.IsAuthenticated)
                {
                    <ul class="navbar-nav">
                        <li class="navbar-nav">
                            <a class="nav-link" asp-action="Index" asp-controller="Member">Üye Sayfası</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        <li class="navbar-nav">
                            <a class="nav-link" asp-action="Logout" asp-controller="Member" asp-route-returnUrl="/Home/Index">Çıkış yap</a>
                        </li>
                    </ul>
                }

                </>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top:10px;">

        @RenderBody()
    </div>
</body>
</html>